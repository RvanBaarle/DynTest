fix {
  fun k9( ) =
    #exit( )
} in
  let _exit = k9 in
    #record-new( ) => y47;
    fix {
      fun skip( ) =
        #print(3) => y48;
        _exit( )
    } in
      #record-write(y47, "skip", skip) => y49;
      #print(1) => y50;
      #record-read(y47, "skip") => y51;
      y51( )