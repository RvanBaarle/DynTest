fix {
  fun k81( ) =
    #exit( )
} in
  #record-new( ) => y365;
  fix {
    fun skip( ) =
      #print(3) => y366;
      k81( )
  } in
    #record-write(y365, "skip", skip) => y367;
    #print(1) => y368;
    fix {
      fun k82( ) =
        k81( )
    } in
      #record-read(y365, "skip") => y369;
      y369( )